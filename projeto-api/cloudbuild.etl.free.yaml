# Cloud Build para dashboard-fg-etl (free tier, Supabase):
#   gcloud builds submit --config projeto-api/cloudbuild.etl.free.yaml projeto-api/

substitutions:
  _REGION: southamerica-east1
  _JOB: dashboard-fg-etl

steps:
  - id: build
    name: gcr.io/cloud-builders/docker
    args:
      - build
      - -f
      - Dockerfile.etl
      - -t
      - ${_REGION}-docker.pkg.dev/$PROJECT_ID/dashboard-fg/etl:$BUILD_ID
      - -t
      - ${_REGION}-docker.pkg.dev/$PROJECT_ID/dashboard-fg/etl:latest
      - .

  - id: push
    name: gcr.io/cloud-builders/docker
    args:
      - push
      - --all-tags
      - ${_REGION}-docker.pkg.dev/$PROJECT_ID/dashboard-fg/etl

  - id: deploy
    name: gcr.io/cloud-builders/gcloud
    args:
      - run
      - jobs
      - deploy
      - ${_JOB}
      - --image=${_REGION}-docker.pkg.dev/$PROJECT_ID/dashboard-fg/etl:$BUILD_ID
      - --region=${_REGION}
      - --task-timeout=1800
      - --memory=1Gi
      - --cpu=2
      - --max-retries=1
      - --set-env-vars=ENV=prod
      - --set-secrets=DATABASE_URL=dashboard-fg-db-url:latest,DATABASE_URL_SYNC=dashboard-fg-db-url-sync:latest,DATAJURI_CLIENT_ID=dashboard-fg-datajuri-client-id:latest,DATAJURI_SECRET=dashboard-fg-datajuri-secret:latest,DATAJURI_USERNAME=dashboard-fg-datajuri-username:latest,DATAJURI_PASSWORD=dashboard-fg-datajuri-password:latest

images:
  - ${_REGION}-docker.pkg.dev/$PROJECT_ID/dashboard-fg/etl

options:
  logging: CLOUD_LOGGING_ONLY
